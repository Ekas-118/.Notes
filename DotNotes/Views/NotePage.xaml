<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DotNotes.Views.NotePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DotNotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="NoteEditor"
                 Text="{x:Bind _noteVm.Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 PlaceholderText="Enter your note"
                 Header="{x:Bind _noteVm.Date.ToString()}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 MaxWidth="400"
                 Grid.Column="1"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="4"
                    Grid.Row="1" Grid.Column="1">
            <Button Content="Save &amp; close" Command="{x:Bind _noteVm.SaveCommand}" Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="Delete" Command="{x:Bind _noteVm.DeleteCommand}"/>
        </StackPanel>
    </Grid>
</Page>
